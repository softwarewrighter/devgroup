# Brief: clean up CLAUDE.md drift -- snolib.plsw is canonical now

**Owner:** dcsno
**Branch:** start as `feat/claude-md-snolib-drift`; `dg-mark-pr` when ready
**Repo:** `sw-cor24-snobol4`
**Drafted by:** mike (forwarded from dcsno's own heads-up after `pr/snolib-extraction` shipped)

## What's wrong

`CLAUDE.md` has an obsolete prohibition in the project-specific section
(below the agentrail-managed autogenerated header at line 153). Lines
173-177 currently say:

> **Do not create `src/snobol4.plsw`, `src/sno_engine.plsw`, or
> `src/snolib.plsw`.** Those names belonged to abandoned single-file
> or alternate-split experiments and are blocked in `.gitignore`.

But `src/snolib.plsw` is **not** abandoned -- it's the canonical
runtime library extracted in the `pr/snolib-extraction` saga (step 1).
`.gitignore` already reflects this correctly:

```
# snolib.plsw is the runtime library, landed in step 1.
# sno_engine.plsw and snobol4.plsw remain blocked.
src/snobol4.plsw
src/sno_engine.plsw
```

So `CLAUDE.md` is the only stale reference. A future agent reading the
prohibition would either (a) refuse to edit `src/snolib.plsw` even
though it's a real source file, or (b) try to delete it.

## What to change

Edit only the project-specific half of `CLAUDE.md` (lines 154-end);
do **not** touch anything inside the
`<!-- agentrail:global:start ... -->` ... `<!-- agentrail:global:end -->`
block (lines 1-153) -- that's regenerated by `agentrail instructions apply`.

Required edits in lines 154-end:

1. **Module list (around line 159):** add `src/snolib.plsw` as the
   runtime library row alongside the four existing modules. Mirror the
   wording from `.gitignore` ("the runtime library").

2. **Prohibition (lines 173-177):** drop `src/snolib.plsw` from the
   "do not create" list. Keep `src/snobol4.plsw` and
   `src/sno_engine.plsw` -- those are still correctly forbidden.
   Optionally add a one-line note clarifying that `snolib.plsw` *was*
   on this list historically and is now canonical (so a future reader
   diffing old branches doesn't re-add it).

3. **`scripts/build-modular.sh` reference (around line 169):** the
   build script now compiles snolib alongside the four modules; update
   the prose to say so. Verify the description still matches what the
   script does.

That's it. Keep the diff tight -- this is a doc-drift fix, not a
re-edit of CLAUDE.md.

## What does NOT belong in this saga

- Any change inside the `agentrail:global` block (autogenerated).
- Editing `.gitignore` -- already correct.
- Refactoring or rewording sections unrelated to the snolib drift.
- Touching docs outside `CLAUDE.md` (`docs/plan.md`, `README.md`, etc.)
  unless they have the same exact drift; if they do, mention it in the
  saga summary so mike can decide whether to expand scope.

## Verification

After the saga lands, the following should all be true:

- `grep -n snolib CLAUDE.md` returns *no* line saying "do not create"
  about it (only descriptive references).
- `grep -n snobol4.plsw\|sno_engine.plsw CLAUDE.md` still finds the
  prohibition for those two abandoned names.
- `agentrail instructions apply` (if dcsno runs it) leaves the
  edits intact -- the changes were only to lines 154-end.
- `just build` still works end-to-end.

## When done

Workflow: `dg-new-feature claude-md-snolib-drift` → edit → `just build`
to confirm nothing broke → commit → `dg-mark-pr` → signal mike.
